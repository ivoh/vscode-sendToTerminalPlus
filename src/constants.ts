export const selectionPatternTag = "{selection}";
export const linePatternTag = "{line}";
export const currentLinePatternTag = "{currentline}";
export const patternTagDelimiter = "\\**xx//";

export const defaultOption = "default";
export const undefinedOption = "undefined";

export enum DelayMode {
    Delayed = "delayed",
    NoDelay = "nodelay",
    Default = "default",
}

export enum PayloadFormat {
    Line = "line",
    Chunk = "chunk",
    All = "all",
    Default = "default",
}

